export enum POST {
    CRUD = "/posts/",
}
export enum AUTH {
    LOGIN="auth/login",
    REFRESH_TOKEN="",
    USER_INFORMATION="user/"
}
export enum USER {
    CREATE="/auth/register",
    ADD_PROFILE="/user/profile_picture",
}